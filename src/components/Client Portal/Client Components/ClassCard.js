import React from 'react';

// will be a styled component eventually

const ClassCard = () => {
    return (
        <div>
            <button>Join</button>
        </div>
    );
};

export default ClassCard;